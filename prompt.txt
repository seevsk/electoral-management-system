El mapa interactivo de /ems/participation sigue sin renderizar bien. La lógica de participación ya funciona y el listado inferior también. NO tocar esa parte.

Problema actual:
- Las cards y lista inferior funcionan.
- El GeoJSON parece cargarse.
- Al seleccionar aparece un distrito como ATE.
- Pero el mapa visualmente no muestra los polígonos reales; se ve casi vacío/mini.
- El problema está en el renderizado SVG manual/proyección/viewBox.

Cambio de enfoque:
Reemplaza el renderizado SVG manual por Leaflet.js usando el GeoJSON real.

Objetivo:
En el bloque “Ámbito Geográfico”, mostrar un mapa real e interactivo de Lima Metropolitana usando Leaflet + GeoJSON.

Reglas:
1. No tocar BD.
2. No tocar migraciones.
3. No tocar login.
4. No tocar votación.
5. No tocar resultados presidenciales.
6. No tocar candidatos, partidos ni elecciones.
7. No tocar el listado inferior de distritos, porque ya funciona.
8. Mantener la lógica actual de votos emitidos, votos pendientes y porcentaje.
9. Solo modificar el bloque del mapa en `src/main/resources/templates/index.html` y CSS si hace falta.

Implementación requerida:
1. Usar Leaflet para renderizar el GeoJSON:
   - cargar CSS/JS de Leaflet en `index.html`
   - crear un div para el mapa, por ejemplo `id="limaMap"`
   - cargar el GeoJSON desde Thymeleaf usando:
     `const geoJsonUrl = /*[[@{/data/lima-distritos.geojson}]]*/ '/ems/data/lima-distritos.geojson';`
2. Renderizar el GeoJSON con `L.geoJSON`.
3. Usar `map.fitBounds(geoJsonLayer.getBounds())` para que Lima Metropolitana se vea completa.
4. Desactivar o limitar zoom si es necesario para que parezca un mapa electoral fijo.
5. Cada distrito debe:
   - tener borde visible
   - tener color según porcentaje de participación
   - mostrar tooltip con nombre del distrito
   - reaccionar al hover
   - poder seleccionarse con click
6. Al hacer click en un distrito:
   - seleccionar ese distrito
   - actualizar los filtros de Departamento/Provincia/Distrito
   - actualizar las tarjetas principales
   - actualizar el donut
   - resaltar el distrito seleccionado
   - sincronizar con la lista inferior
7. Relacionar el distrito del GeoJSON con la data del backend usando nombre normalizado:
   - ignorar mayúsculas/minúsculas
   - ignorar tildes
   - ignorar espacios dobles
8. Si el GeoJSON trae Callao u otras zonas, renderizar solo los distritos que existan en `districtStats` o en la data de participación del backend.
9. Si Leaflet/GeoJSON falla, mostrar mensaje solo dentro del bloque del mapa, pero la pantalla debe seguir cargando.

Importante:
Ya no quiero un SVG manual calculado con viewBox.
Quiero Leaflet + GeoJSON porque el SVG manual no está dibujando los polígonos correctamente.

Diseño:
- Mantener el estilo actual de la card.
- El mapa debe ocupar bien el bloque izquierdo.
- Altura sugerida: 360px o 420px.
- Fondo claro.
- Bordes suaves.
- Sin resultados de candidatos.

Archivos permitidos:
- `src/main/resources/templates/index.html`
- `src/main/resources/static/css/estilo.css` solo si hace falta.

Al terminar:
- Compilar con `mvn -q -DskipTests compile`.
- Indicar qué archivos tocaste.
- Indicar cómo probar en `/ems/participation`.